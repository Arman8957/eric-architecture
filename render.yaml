services:
  - type: web
    name: nestjs-backend
    runtime: node
    region: singapore # closest stable region to Bangladesh
    plan: free

    buildCommand: |
      npm install
      npm run build

    startCommand: npm run start:prod

    envVars:
      - key: NODE_ENV
        value: production

      - key: PORT
        value: 10000
